{
  "mcpServers": {
    "milvus": {
      "type": "stdio",
      "command": "uvx",
      "args": ["milvus-mcp"],
      "env": {
        "MILVUS_URI": "${MILVUS_URI}",
        "MILVUS_TOKEN": "${MILVUS_TOKEN}"
      }
    }
  }
}
